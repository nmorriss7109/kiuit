import React, { Component } from 'react';

class About extends Component {
  render() {
    return(
      <div className="About">
        <p>
            This app allows you to create and join rooms, in which participants can add songs to the host's queue.
        </p>
      </div>
    );
  }
}

export default About;